const config = {
    content: ['./src/**/*.{html,js,svelte,ts}', '../../packages/components/src/**/*.{html,js,svelte,ts}'],

    theme: {
        extend: {}
    },

    plugins: []
};

module.exports = config;
